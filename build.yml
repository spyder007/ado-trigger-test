trigger:
  tags:
    include:
    - "*"
  branches:
    include:
    - feature/*
pr:
- main

pool:
  name: Default

steps:
- script: |
    echo $(Build.Reason)
    echo $(Build.SourceBranchName)
    echo $(Build.SourceBranch)